---
layout: base.njk
title: The Framework
door: framework
permalink: /framework/
description: "Ho System definitions, project documents, and downloadable Markdown templates."
---
<h1>The Framework</h1>

<p>The framework documents define how Ho projects are planned, carried out, checked, and recorded. Use the diagram to follow the project documents, or browse the definitions and download templates below.</p>

<figure class="chain-figure">
  {# §6 scale honesty: emit BOTH static SVGs and CSS-toggle on the rendered column width.
     Below a 480 px column the full diagram would have to shrink past legibility, so we show
     §6's header-scale chain instead of scaling the full one down. The header variant carries
     no per-node door targets by frozen definition (chain.header() takes no links), so the
     whole compact diagram links to the full chain's position on this page (#kamae-chain). #}
  <div class="chain-full" id="kamae-chain">
    {% chainSvg "full", corpus.chainLinks %}
  </div>
  <a class="chain-header-link" href="#kamae-chain" aria-label="The Kamae chain — six links: four framing documents, a pre-action document per session, and the living State Memory">
    {% chainSvg "header" %}
  </a>
  <figcaption class="caption">The Kamae chain begins with Seed, System Design, README, and Ho Overview. Per-ho documents define individual working sessions; State Memory records the project's current position between sessions. Later work can lead you to revise an earlier document; record the revision and its reason.</figcaption>
</figure>

<h2>The documents</h2>
<ul class="doc-list">
  {%- for d in corpus.frameworkDocs %}
  <li><a href="{{ d.url }}">{{ d.title }}</a> <span class="meta">{% if d.frontmatter.id %}{{ d.frontmatter.id }} · {% endif %}{{ d.frontmatter.type or "" }}</span></li>
  {%- endfor %}
</ul>
<p class="caption">The <a class="chain-link" href="{{ corpus.documentsIndex.url }}">index of documents</a> lists the framework's documents and their source locations.</p>

<h2 id="templates">Templates</h2>
<p>Download a Markdown template to begin a project document. The <a class="chain-link" href="/skills/">Ho skills</a> can guide you through developing these documents.</p>
<ul class="tmpl-list">
  {%- for t in corpus.templates %}
  <li><span>{{ t.title }}</span> <span class="dl"><a class="chain-link" href="{{ t.href }}" download>download .md</a> · <a class="chain-link" href="{{ t.github }}">source</a></span></li>
  {%- endfor %}
</ul>
